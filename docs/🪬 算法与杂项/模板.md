# 模板

## 程序格式(codeforces)

```cpp
#include <bits/stdc++.h>

using namespace std;
using ll = long long;

void solve(void) {
	/* Coding begins here. */
}

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	
	ll test;
	while (test --) {
		solve();
	}

	return 0;
}
```
