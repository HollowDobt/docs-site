# Lec X: 杂项

- 与 `C++` 不同, `Java` 中的参数传递仅有**值传递**. 并且这一**值传递**和 `C++` 中的意思不同, 指的是原变量存的是什么就传递什么. 具体而言, 对于基本类型(如 `int`, `short` 等)是传值, 而对于引用类型(如列表)是传地址.
- 对外暴露的涉及递归的方法(因为传值的需要)往往需要一个另外的内部的 `private` 方法实现.
- 关于环形链表, 只需要一个哨兵节点就够了, 其 `prev` 为最后一个节点, `next` 为第一个节点.